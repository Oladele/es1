<ul>
	<li>
		Name: {{model.name}}
		{{#link-to 'companies.edit' model }}	
			(Edit)
		{{/link-to}}
		<a href="#" {{action "delete" model}}>(Delete)</a>
	</li> 
</ul>

{{link-to 'Companies List' 'companies'}}